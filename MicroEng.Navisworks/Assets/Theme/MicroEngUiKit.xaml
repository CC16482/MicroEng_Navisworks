<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                    xmlns:s="clr-namespace:System;assembly=mscorlib">
  <!--
    Legacy MicroEng style tokens.
    Kept for backwards compatibility with older XAML references.
    New UI should prefer WPF-UI resources where possible.
  -->

  <!-- Brushes -->
  <!-- Prefer WPF-UI's design-guidance colors (see Wpf.Ui.Gallery ColorsPage). -->
  <!--
    NOTE: Do NOT declare <DynamicResource .../> as a resource value.
    That creates a DynamicResourceExtension object (not a Brush) and causes runtime XamlParseException
    ("Provide value on StaticResourceHolder...").
    Instead, create real brushes whose Color comes from WPF-UI dynamic theme resources.
  -->
  <SolidColorBrush x:Key="AppBackgroundBrush" Color="{DynamicResource ApplicationBackgroundColor}" />
  <SolidColorBrush x:Key="CardBackgroundBrush" Color="{DynamicResource CardBackgroundFillColorDefault}" />
  <SolidColorBrush x:Key="CardBorderBrush" Color="{DynamicResource ControlStrokeColorDefault}" />
  <SolidColorBrush x:Key="TextPrimaryBrush" Color="{DynamicResource TextFillColorPrimary}" />
  <SolidColorBrush x:Key="TextPrimary" Color="{DynamicResource TextFillColorPrimary}" />
  <SolidColorBrush x:Key="TextSecondaryBrush" Color="{DynamicResource TextFillColorSecondary}" />
  <SolidColorBrush x:Key="TextSecondary" Color="{DynamicResource TextFillColorSecondary}" />
  <!-- Ensure primary/secondary text brushes exist even if theme dictionaries fail to load. -->
  <SolidColorBrush x:Key="TextFillColorPrimaryBrush" Color="{DynamicResource TextFillColorPrimary}" />
  <SolidColorBrush x:Key="TextFillColorSecondaryBrush" Color="{DynamicResource TextFillColorSecondary}" />
  <!-- Use the base accent so MicroEng "accent" stays consistent across controls (matches Gallery expectation). -->
  <SolidColorBrush x:Key="AccentBrush" Color="{DynamicResource SystemAccentColor}" />
  <SolidColorBrush x:Key="AccentStrongBrush" Color="{DynamicResource SystemAccentColor}" />

  <!-- DataGrid grid lines -->
  <Color x:Key="MicroEngDataGridGridLineColor">#C0C0C0</Color>
  <SolidColorBrush x:Key="MicroEngDataGridGridLineBrush"
                   Color="{DynamicResource MicroEngDataGridGridLineColor}"
                   Opacity="0.55" />

  <!-- Spacing -->
  <Thickness x:Key="PagePadding">12</Thickness>
  <Thickness x:Key="SectionPadding">10</Thickness>
  <Thickness x:Key="SectionMargin">0,0,0,8</Thickness>

  <!-- Typography -->
  <Style x:Key="PageTitleText" TargetType="TextBlock">
    <Setter Property="FontSize" Value="18" />
    <Setter Property="FontWeight" Value="SemiBold" />
    <!-- Use WPF-UI brush directly so theme switching updates correctly. -->
    <Setter Property="Foreground" Value="{DynamicResource TextFillColorPrimaryBrush}" />
    <Setter Property="Margin" Value="0,0,0,6" />
  </Style>

  <Style x:Key="SectionTitleText" TargetType="TextBlock">
    <Setter Property="FontSize" Value="14" />
    <Setter Property="FontWeight" Value="SemiBold" />
    <Setter Property="Foreground" Value="{DynamicResource TextFillColorPrimaryBrush}" />
    <Setter Property="Margin" Value="0,0,0,6" />
  </Style>

  <Style x:Key="SubtleText" TargetType="TextBlock">
    <Setter Property="Foreground" Value="{DynamicResource TextFillColorSecondaryBrush}" />
    <Setter Property="FontSize" Value="12" />
  </Style>

  <!--
    IMPORTANT:
    Many MicroEng views still use plain WPF TextBlock/Label for form labels.
    WPF-UI sets themed Foregrounds via Control.Foreground, but TextBlock doesn't inherit that reliably,
    which can leave text black-on-dark (or white-on-light).
    Provide a safe implicit Foreground for these text elements based on WPF-UI theme colors.
  -->
  <Style TargetType="TextBlock">
    <Setter Property="Foreground" Value="{DynamicResource TextFillColorPrimaryBrush}" />
  </Style>

  <Style TargetType="Label">
    <Setter Property="Foreground" Value="{DynamicResource TextFillColorPrimaryBrush}" />
  </Style>

  <Style x:Key="MonoText" TargetType="TextBox">
    <Setter Property="FontFamily" Value="Consolas" />
    <Setter Property="FontSize" Value="11" />
  </Style>

  <!-- Card -->
  <Style x:Key="CardBorder" TargetType="Border">
    <Setter Property="Background" Value="{DynamicResource CardBackgroundFillColorDefaultBrush}" />
    <Setter Property="BorderBrush" Value="{DynamicResource ControlStrokeColorDefaultBrush}" />
    <Setter Property="BorderThickness" Value="1" />
    <Setter Property="CornerRadius" Value="8" />
    <Setter Property="Padding" Value="{StaticResource SectionPadding}" />
    <Setter Property="Margin" Value="{StaticResource SectionMargin}" />
  </Style>

  <!-- Buttons -->
  <Style x:Key="PrimaryButtonStyle" TargetType="Button">
    <Setter Property="Padding" Value="12,6" />
    <Setter Property="Margin" Value="4" />
    <!-- Prefer WPF-UI tokens (avoids "white on white" when accent is set to white). -->
    <Setter Property="Foreground" Value="{DynamicResource TextOnAccentFillColorPrimaryBrush}" />
    <Setter Property="Background" Value="{DynamicResource AccentFillColorDefaultBrush}" />
    <Setter Property="BorderBrush" Value="{DynamicResource AccentFillColorDefaultBrush}" />
    <Setter Property="BorderThickness" Value="1" />
    <Setter Property="FontWeight" Value="SemiBold" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="Button">
          <Border Background="{TemplateBinding Background}"
                  BorderBrush="{TemplateBinding BorderBrush}"
                  BorderThickness="{TemplateBinding BorderThickness}"
                  CornerRadius="6"
                  Padding="{TemplateBinding Padding}">
            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
          </Border>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style x:Key="PrimaryButton" TargetType="Button" BasedOn="{StaticResource PrimaryButtonStyle}" />

  <Style x:Key="SecondaryButtonStyle" TargetType="Button" BasedOn="{StaticResource PrimaryButtonStyle}">
    <Setter Property="Foreground" Value="{DynamicResource TextFillColorPrimaryBrush}" />
    <Setter Property="Background" Value="{DynamicResource ControlFillColorDefaultBrush}" />
    <Setter Property="BorderBrush" Value="{DynamicResource ControlStrokeColorDefaultBrush}" />
  </Style>

  <Style x:Key="SecondaryButton" TargetType="Button" BasedOn="{StaticResource SecondaryButtonStyle}" />

  <Style x:Key="ToolbarButtonStyle" TargetType="Button" BasedOn="{StaticResource SecondaryButtonStyle}">
    <Setter Property="Padding" Value="10,4" />
    <Setter Property="Margin" Value="2" />
  </Style>

  <!-- Inputs -->
  <!--
    NOTE: Do NOT override implicit styles for TextBox/ComboBox/CheckBox/RadioButton.
    WPF-UI already provides Gallery-consistent styles (ComboBoxPage/RadioButtonPage/etc).
    Overriding them removes accents and breaks the WPF-UI look.
  -->
  <!--
    Exception: WPF-UI CheckBox animates ControlIcon.Tag, which can crash during template invalidation.
    Override the default CheckBox style to animate ScaleTransform instead of Tag.
  -->
  <Style x:Key="DefaultCheckBoxStyle" TargetType="CheckBox">
    <Setter Property="FrameworkElement.FocusVisualStyle">
      <Setter.Value>
        <DynamicResource ResourceKey="DefaultControlFocusVisualStyle" />
      </Setter.Value>
    </Setter>
    <Setter Property="Panel.Background">
      <Setter.Value>
        <DynamicResource ResourceKey="CheckBoxBackground" />
      </Setter.Value>
    </Setter>
    <Setter Property="TextElement.Foreground">
      <Setter.Value>
        <DynamicResource ResourceKey="CheckBoxForeground" />
      </Setter.Value>
    </Setter>
    <Setter Property="Border.BorderBrush">
      <Setter.Value>
        <DynamicResource ResourceKey="CheckBoxBorderBrush" />
      </Setter.Value>
    </Setter>
    <Setter Property="Border.BorderThickness">
      <Setter.Value>
        <Thickness>1,1,1,1</Thickness>
      </Setter.Value>
    </Setter>
    <Setter Property="Control.Padding">
      <Setter.Value>
        <Thickness>11,5,11,6</Thickness>
      </Setter.Value>
    </Setter>
    <Setter Property="Border.CornerRadius">
      <Setter.Value>
        <DynamicResource ResourceKey="ControlCornerRadius" />
      </Setter.Value>
    </Setter>
    <Setter Property="FrameworkElement.HorizontalAlignment">
      <Setter.Value>
        <x:Static Member="HorizontalAlignment.Left" />
      </Setter.Value>
    </Setter>
    <Setter Property="FrameworkElement.VerticalAlignment">
      <Setter.Value>
        <x:Static Member="VerticalAlignment.Center" />
      </Setter.Value>
    </Setter>
    <Setter Property="Control.HorizontalContentAlignment">
      <Setter.Value>
        <x:Static Member="HorizontalAlignment.Left" />
      </Setter.Value>
    </Setter>
    <Setter Property="Control.VerticalContentAlignment">
      <Setter.Value>
        <x:Static Member="VerticalAlignment.Center" />
      </Setter.Value>
    </Setter>
    <Setter Property="FrameworkElement.Cursor">
      <Setter.Value>
        <Cursor>Hand</Cursor>
      </Setter.Value>
    </Setter>
    <Setter Property="TextElement.FontSize">
      <Setter.Value>
        <DynamicResource ResourceKey="ControlContentThemeFontSize" />
      </Setter.Value>
    </Setter>
    <Setter Property="TextElement.FontWeight">
      <Setter.Value>
        <FontWeight>Normal</FontWeight>
      </Setter.Value>
    </Setter>
    <Setter Property="KeyboardNavigation.IsTabStop">
      <Setter.Value>
        <s:Boolean>True</s:Boolean>
      </Setter.Value>
    </Setter>
    <Setter Property="UIElement.Focusable">
      <Setter.Value>
        <s:Boolean>True</s:Boolean>
      </Setter.Value>
    </Setter>
    <Setter Property="FrameworkElement.MinWidth">
      <Setter.Value>
        <s:Double>120</s:Double>
      </Setter.Value>
    </Setter>
    <Setter Property="FrameworkElement.MinHeight">
      <Setter.Value>
        <s:Double>32</s:Double>
      </Setter.Value>
    </Setter>
    <Setter Property="UIElement.SnapsToDevicePixels">
      <Setter.Value>
        <s:Boolean>True</s:Boolean>
      </Setter.Value>
    </Setter>
    <Setter Property="FrameworkElement.OverridesDefaultStyle">
      <Setter.Value>
        <s:Boolean>True</s:Boolean>
      </Setter.Value>
    </Setter>
    <Setter Property="Control.Template">
      <Setter.Value>
        <ControlTemplate TargetType="CheckBox">
          <BulletDecorator Background="#00FFFFFF"
                           Margin="{TemplateBinding Control.Padding}"
                           HorizontalAlignment="{TemplateBinding FrameworkElement.HorizontalAlignment}"
                           VerticalAlignment="{TemplateBinding FrameworkElement.VerticalAlignment}">
            <BulletDecorator.Bullet>
              <Border CornerRadius="{TemplateBinding Border.CornerRadius}"
                      Background="{TemplateBinding Panel.Background}"
                      Name="ControlBorderIconPresenter"
                      Width="{DynamicResource CheckBoxHeight}"
                      Height="{DynamicResource CheckBoxWidth}"
                      HorizontalAlignment="Left"
                      VerticalAlignment="Center">
                <Border BorderThickness="{TemplateBinding Border.BorderThickness}"
                        CornerRadius="{TemplateBinding Border.CornerRadius}"
                        BorderBrush="{TemplateBinding Border.BorderBrush}"
                        Name="StrokeBorder">
                  <ui:SymbolIcon Symbol="Checkmark48"
                                 FontFamily="{DynamicResource FluentSystemIcons}"
                                 FontSize="{DynamicResource CheckBoxIconSize}"
                                 FontWeight="Bold"
                                 Foreground="{DynamicResource CheckBoxCheckGlyphForeground}"
                                 Name="ControlIcon"
                                 HorizontalAlignment="Center"
                                 VerticalAlignment="Center"
                                 Visibility="Collapsed"
                                 RenderTransformOrigin="0.5,0.5">
                    <ui:SymbolIcon.RenderTransform>
                      <ScaleTransform ScaleX="1" ScaleY="1" />
                    </ui:SymbolIcon.RenderTransform>
                  </ui:SymbolIcon>
                </Border>
              </Border>
            </BulletDecorator.Bullet>
            <ContentPresenter RecognizesAccessKey="True"
                              Content="{TemplateBinding ContentControl.Content}"
                              ContentTemplate="{TemplateBinding ContentControl.ContentTemplate}"
                              ContentStringFormat="{TemplateBinding ContentControl.ContentStringFormat}"
                              Name="ContentPresenter"
                              Margin="8,0,0,0"
                              HorizontalAlignment="{TemplateBinding Control.HorizontalContentAlignment}"
                              VerticalAlignment="{TemplateBinding Control.VerticalContentAlignment}" />
          </BulletDecorator>
          <ControlTemplate.Triggers>
            <Trigger Property="ContentControl.Content" Value="{x:Null}">
              <Setter Property="FrameworkElement.Margin" TargetName="ContentPresenter">
                <Setter.Value>
                  <Thickness>0,0,0,0</Thickness>
                </Setter.Value>
              </Setter>
              <Setter Property="FrameworkElement.MinWidth">
                <Setter.Value>
                  <s:Double>30</s:Double>
                </Setter.Value>
              </Setter>
            </Trigger>
            <Trigger Property="ContentControl.Content" Value="">
              <Setter Property="FrameworkElement.Margin" TargetName="ContentPresenter">
                <Setter.Value>
                  <Thickness>0,0,0,0</Thickness>
                </Setter.Value>
              </Setter>
              <Setter Property="FrameworkElement.MinWidth">
                <Setter.Value>
                  <s:Double>30</s:Double>
                </Setter.Value>
              </Setter>
            </Trigger>
            <Trigger Property="ToggleButton.IsChecked">
              <Setter Property="ui:SymbolIcon.Symbol" TargetName="ControlIcon">
                <Setter.Value>
                  <x:Static Member="ui:SymbolRegular.Subtract16" />
                </Setter.Value>
              </Setter>
              <Setter Property="UIElement.Visibility" TargetName="ControlIcon">
                <Setter.Value>
                  <x:Static Member="Visibility.Visible" />
                </Setter.Value>
              </Setter>
              <Setter Property="Panel.Background" TargetName="ControlBorderIconPresenter">
                <Setter.Value>
                  <DynamicResource ResourceKey="CheckBoxCheckBackgroundFillChecked" />
                </Setter.Value>
              </Setter>
              <Setter Property="Border.BorderBrush" TargetName="StrokeBorder">
                <Setter.Value>
                  <DynamicResource ResourceKey="CheckBoxCheckBorderBrush" />
                </Setter.Value>
              </Setter>
              <Trigger.Value>
                <x:Null />
              </Trigger.Value>
            </Trigger>
            <Trigger Property="ToggleButton.IsChecked">
              <Trigger.EnterActions>
                <BeginStoryboard Name="SlideIn">
                  <Storyboard>
                    <DoubleAnimation From="0"
                                     To="1"
                                     Duration="00:00:00.2500000"
                                     Storyboard.TargetName="ControlIcon"
                                     Storyboard.TargetProperty="RenderTransform.(ScaleTransform.ScaleX)">
                      <DoubleAnimation.EasingFunction>
                        <CubicEase EasingMode="EaseOut" />
                      </DoubleAnimation.EasingFunction>
                    </DoubleAnimation>
                    <DoubleAnimation From="0"
                                     To="1"
                                     Duration="00:00:00.2500000"
                                     Storyboard.TargetName="ControlIcon"
                                     Storyboard.TargetProperty="RenderTransform.(ScaleTransform.ScaleY)">
                      <DoubleAnimation.EasingFunction>
                        <CubicEase EasingMode="EaseOut" />
                      </DoubleAnimation.EasingFunction>
                    </DoubleAnimation>
                  </Storyboard>
                </BeginStoryboard>
              </Trigger.EnterActions>
              <Setter Property="UIElement.Visibility" TargetName="ControlIcon">
                <Setter.Value>
                  <x:Static Member="Visibility.Visible" />
                </Setter.Value>
              </Setter>
              <Setter Property="Panel.Background" TargetName="ControlBorderIconPresenter">
                <Setter.Value>
                  <DynamicResource ResourceKey="CheckBoxCheckBackgroundFillChecked" />
                </Setter.Value>
              </Setter>
              <Setter Property="Border.BorderBrush" TargetName="StrokeBorder">
                <Setter.Value>
                  <DynamicResource ResourceKey="CheckBoxCheckBorderBrush" />
                </Setter.Value>
              </Setter>
              <Trigger.Value>
                <s:Boolean>True</s:Boolean>
              </Trigger.Value>
            </Trigger>
            <Trigger Property="UIElement.Visibility">
              <Trigger.EnterActions>
                <SeekStoryboard Offset="00:00:00.2500000" BeginStoryboardName="SlideIn" />
              </Trigger.EnterActions>
              <Trigger.Value>
                <x:Static Member="Visibility.Visible" />
              </Trigger.Value>
            </Trigger>
            <EventTrigger RoutedEvent="FrameworkElement.Loaded">
              <SeekStoryboard Offset="00:00:00.2500000" BeginStoryboardName="SlideIn" />
            </EventTrigger>
            <MultiTrigger>
              <MultiTrigger.Conditions>
                <Condition Property="UIElement.IsMouseOver">
                  <Condition.Value>
                    <s:Boolean>True</s:Boolean>
                  </Condition.Value>
                </Condition>
                <Condition Property="ButtonBase.IsPressed">
                  <Condition.Value>
                    <s:Boolean>False</s:Boolean>
                  </Condition.Value>
                </Condition>
                <Condition Property="ToggleButton.IsChecked">
                  <Condition.Value>
                    <s:Boolean>False</s:Boolean>
                  </Condition.Value>
                </Condition>
              </MultiTrigger.Conditions>
              <Setter Property="Panel.Background" TargetName="StrokeBorder">
                <Setter.Value>
                  <DynamicResource ResourceKey="CheckBoxCheckBackgroundFillUncheckedPointerOver" />
                </Setter.Value>
              </Setter>
            </MultiTrigger>
            <MultiTrigger>
              <MultiTrigger.Conditions>
                <Condition Property="UIElement.IsMouseOver">
                  <Condition.Value>
                    <s:Boolean>True</s:Boolean>
                  </Condition.Value>
                </Condition>
                <Condition Property="ButtonBase.IsPressed">
                  <Condition.Value>
                    <s:Boolean>True</s:Boolean>
                  </Condition.Value>
                </Condition>
                <Condition Property="ToggleButton.IsChecked">
                  <Condition.Value>
                    <s:Boolean>False</s:Boolean>
                  </Condition.Value>
                </Condition>
              </MultiTrigger.Conditions>
              <Setter Property="Panel.Background" TargetName="StrokeBorder">
                <Setter.Value>
                  <DynamicResource ResourceKey="CheckBoxCheckBackgroundFillUncheckedPressed" />
                </Setter.Value>
              </Setter>
            </MultiTrigger>
            <MultiTrigger>
              <MultiTrigger.Conditions>
                <Condition Property="UIElement.IsMouseOver">
                  <Condition.Value>
                    <s:Boolean>True</s:Boolean>
                  </Condition.Value>
                </Condition>
                <Condition Property="ButtonBase.IsPressed">
                  <Condition.Value>
                    <s:Boolean>False</s:Boolean>
                  </Condition.Value>
                </Condition>
                <Condition Property="ToggleButton.IsChecked" Value="{x:Null}" />
              </MultiTrigger.Conditions>
              <Setter Property="Panel.Background" TargetName="ControlBorderIconPresenter">
                <Setter.Value>
                  <DynamicResource ResourceKey="CheckBoxCheckBackgroundFillCheckedPointerOver" />
                </Setter.Value>
              </Setter>
              <Setter Property="Border.BorderBrush" TargetName="StrokeBorder">
                <Setter.Value>
                  <DynamicResource ResourceKey="CheckBoxCheckBorderBrush" />
                </Setter.Value>
              </Setter>
            </MultiTrigger>
            <MultiTrigger>
              <MultiTrigger.Conditions>
                <Condition Property="UIElement.IsMouseOver">
                  <Condition.Value>
                    <s:Boolean>True</s:Boolean>
                  </Condition.Value>
                </Condition>
                <Condition Property="ButtonBase.IsPressed">
                  <Condition.Value>
                    <s:Boolean>True</s:Boolean>
                  </Condition.Value>
                </Condition>
                <Condition Property="ToggleButton.IsChecked" Value="{x:Null}" />
              </MultiTrigger.Conditions>
              <Setter Property="Panel.Background" TargetName="ControlBorderIconPresenter">
                <Setter.Value>
                  <DynamicResource ResourceKey="CheckBoxCheckBackgroundFillCheckedPressed" />
                </Setter.Value>
              </Setter>
              <Setter Property="Border.BorderBrush" TargetName="StrokeBorder">
                <Setter.Value>
                  <DynamicResource ResourceKey="CheckBoxCheckBorderBrush" />
                </Setter.Value>
              </Setter>
            </MultiTrigger>
            <MultiTrigger>
              <MultiTrigger.Conditions>
                <Condition Property="UIElement.IsMouseOver">
                  <Condition.Value>
                    <s:Boolean>True</s:Boolean>
                  </Condition.Value>
                </Condition>
                <Condition Property="ButtonBase.IsPressed">
                  <Condition.Value>
                    <s:Boolean>False</s:Boolean>
                  </Condition.Value>
                </Condition>
                <Condition Property="ToggleButton.IsChecked">
                  <Condition.Value>
                    <s:Boolean>True</s:Boolean>
                  </Condition.Value>
                </Condition>
              </MultiTrigger.Conditions>
              <Setter Property="Panel.Background" TargetName="ControlBorderIconPresenter">
                <Setter.Value>
                  <DynamicResource ResourceKey="CheckBoxCheckBackgroundFillCheckedPointerOver" />
                </Setter.Value>
              </Setter>
              <Setter Property="Border.BorderBrush" TargetName="StrokeBorder">
                <Setter.Value>
                  <DynamicResource ResourceKey="CheckBoxCheckBorderBrush" />
                </Setter.Value>
              </Setter>
            </MultiTrigger>
            <MultiTrigger>
              <MultiTrigger.Conditions>
                <Condition Property="UIElement.IsMouseOver">
                  <Condition.Value>
                    <s:Boolean>True</s:Boolean>
                  </Condition.Value>
                </Condition>
                <Condition Property="ButtonBase.IsPressed">
                  <Condition.Value>
                    <s:Boolean>True</s:Boolean>
                  </Condition.Value>
                </Condition>
                <Condition Property="ToggleButton.IsChecked">
                  <Condition.Value>
                    <s:Boolean>True</s:Boolean>
                  </Condition.Value>
                </Condition>
              </MultiTrigger.Conditions>
              <Setter Property="Panel.Background" TargetName="ControlBorderIconPresenter">
                <Setter.Value>
                  <DynamicResource ResourceKey="CheckBoxCheckBackgroundFillCheckedPressed" />
                </Setter.Value>
              </Setter>
              <Setter Property="Border.BorderBrush" TargetName="StrokeBorder">
                <Setter.Value>
                  <DynamicResource ResourceKey="CheckBoxCheckBorderBrush" />
                </Setter.Value>
              </Setter>
            </MultiTrigger>
            <Trigger Property="UIElement.IsEnabled">
              <Setter Property="Panel.Background" TargetName="ControlBorderIconPresenter">
                <Setter.Value>
                  <DynamicResource ResourceKey="CheckBoxCheckBackgroundFillUncheckedDisabled" />
                </Setter.Value>
              </Setter>
              <Setter Property="Border.BorderBrush" TargetName="StrokeBorder">
                <Setter.Value>
                  <DynamicResource ResourceKey="CheckBoxCheckBackgroundStrokeUncheckedDisabled" />
                </Setter.Value>
              </Setter>
              <Setter Property="TextElement.Foreground" TargetName="ControlIcon">
                <Setter.Value>
                  <DynamicResource ResourceKey="CheckBoxForegroundUncheckedDisabled" />
                </Setter.Value>
              </Setter>
              <Setter Property="TextElement.Foreground">
                <Setter.Value>
                  <DynamicResource ResourceKey="TextFillColorDisabledBrush" />
                </Setter.Value>
              </Setter>
              <Trigger.Value>
                <s:Boolean>False</s:Boolean>
              </Trigger.Value>
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style TargetType="CheckBox" BasedOn="{StaticResource DefaultCheckBoxStyle}" />

  <!--
    DataGrids:
    Do NOT override WPF-UI's implicit ui:DataGrid style (it provides the dark/light visuals).
    Instead, set gridline brushes directly on each DataGrid instance where needed.
  -->
</ResourceDictionary>
