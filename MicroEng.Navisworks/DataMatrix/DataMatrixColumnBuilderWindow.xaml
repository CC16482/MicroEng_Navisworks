<Window x:Class="MicroEng.Navisworks.DataMatrixColumnBuilderWindow"
        x:ClassModifier="internal"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:system="clr-namespace:System;assembly=System"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:local="clr-namespace:MicroEng.Navisworks"
        Title="Column Builder"
        Width="980"
        Height="640"
        MinWidth="750"
        MinHeight="560"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize"
        Topmost="True"
        Background="{DynamicResource ApplicationBackgroundBrush}"
        Foreground="{DynamicResource TextFillColorPrimaryBrush}"
        FontSize="12">
    <Window.Resources>
        <ResourceDictionary>
            <system:Double x:Key="ControlContentThemeFontSize">12</system:Double>
            <BooleanToVisibilityConverter x:Key="BoolToVisibility" />
            <SolidColorBrush x:Key="ColumnBuilderCountBrush" Color="#2F80FF" />
            <SolidColorBrush x:Key="ColumnBuilderRequiredBrush" Color="#FF8C00" />
            <!-- Column Builder only: lightweight CheckBox template (no animations) to avoid WPF-UI animation crashes and reduce scroll overhead -->
            <Style x:Key="ColumnBuilderTreeCheckBoxStyle" TargetType="{x:Type CheckBox}">
                <Setter Property="Focusable" Value="False" />
                <Setter Property="IsTabStop" Value="False" />
                <Setter Property="SnapsToDevicePixels" Value="True" />
                <Setter Property="Foreground" Value="{DynamicResource TextFillColorPrimaryBrush}" />
                <Setter Property="Background" Value="Transparent" />
                <Setter Property="HorizontalContentAlignment" Value="Left" />
                <Setter Property="VerticalAlignment" Value="Center" />
                <Setter Property="VerticalContentAlignment" Value="Center" />
                <Setter Property="MinHeight" Value="22" />
                <Setter Property="Padding" Value="0" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type CheckBox}">
                            <Grid SnapsToDevicePixels="True" VerticalAlignment="Center">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="20" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <Grid Grid.Column="0"
                                      Width="20"
                                      Height="20"
                                      VerticalAlignment="Center"
                                      HorizontalAlignment="Left">
                                    <Border x:Name="Box"
                                            CornerRadius="4"
                                            BorderThickness="1"
                                            BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}"
                                            Background="{DynamicResource ControlFillColorDefaultBrush}" />
                                    <Path x:Name="CheckMark"
                                          Data="M 4 10 L 8.2 14 L 16 6"
                                          Stroke="{DynamicResource TextOnAccentFillColorPrimaryBrush}"
                                          StrokeThickness="2.2"
                                          StrokeStartLineCap="Round"
                                          StrokeEndLineCap="Round"
                                          Visibility="Collapsed" />
                                    <Border x:Name="IndeterminateMark"
                                            Width="12"
                                            Height="2.6"
                                            CornerRadius="2"
                                            Background="{DynamicResource TextOnAccentFillColorPrimaryBrush}"
                                            HorizontalAlignment="Center"
                                            VerticalAlignment="Center"
                                            Visibility="Collapsed" />
                                </Grid>
                                <ContentPresenter x:Name="ContentPresenter"
                                                  Grid.Column="1"
                                                  Margin="8,0,0,0"
                                                  RecognizesAccessKey="True"
                                                  Content="{TemplateBinding Content}"
                                                  ContentTemplate="{TemplateBinding ContentTemplate}"
                                                  ContentStringFormat="{TemplateBinding ContentStringFormat}"
                                                  HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                  VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                                  SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                            </Grid>

                            <ControlTemplate.Triggers>
                                <Trigger Property="Content" Value="{x:Null}">
                                    <Setter TargetName="ContentPresenter" Property="Margin" Value="0" />
                                    <Setter TargetName="ContentPresenter" Property="Visibility" Value="Collapsed" />
                                </Trigger>
                                <Trigger Property="Content" Value="">
                                    <Setter TargetName="ContentPresenter" Property="Margin" Value="0" />
                                    <Setter TargetName="ContentPresenter" Property="Visibility" Value="Collapsed" />
                                </Trigger>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Box" Property="Background" Value="{DynamicResource ControlFillColorSecondaryBrush}" />
                                </Trigger>
                                <Trigger Property="IsChecked" Value="True">
                                    <Setter TargetName="Box" Property="Background" Value="{DynamicResource AccentFillColorDefaultBrush}" />
                                    <Setter TargetName="Box" Property="BorderBrush" Value="{DynamicResource AccentFillColorSecondaryBrush}" />
                                    <Setter TargetName="CheckMark" Property="Visibility" Value="Visible" />
                                    <Setter TargetName="IndeterminateMark" Property="Visibility" Value="Collapsed" />
                                </Trigger>
                                <Trigger Property="IsChecked" Value="{x:Null}">
                                    <Setter TargetName="Box" Property="Background" Value="{DynamicResource AccentFillColorDefaultBrush}" />
                                    <Setter TargetName="Box" Property="BorderBrush" Value="{DynamicResource AccentFillColorSecondaryBrush}" />
                                    <Setter TargetName="IndeterminateMark" Property="Visibility" Value="Visible" />
                                    <Setter TargetName="CheckMark" Property="Visibility" Value="Collapsed" />
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter Property="Opacity" Value="0.55" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- Column Builder tree expander: minimal toggle chrome (arrow is provided by content) -->
            <Style x:Key="ExpandCollapseToggleStyle" TargetType="{x:Type ToggleButton}">
                <Setter Property="Focusable" Value="False" />
                <Setter Property="IsTabStop" Value="False" />
                <Setter Property="Width" Value="16" />
                <Setter Property="Height" Value="16" />
                <Setter Property="Background" Value="Transparent" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type ToggleButton}">
                            <Border Background="{TemplateBinding Background}"
                                    Width="{TemplateBinding Width}"
                                    Height="{TemplateBinding Height}"
                                    SnapsToDevicePixels="True">
                                <ContentPresenter HorizontalAlignment="Center"
                                                  VerticalAlignment="Center" />
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <ControlTemplate x:Key="ChooseTreeItemTemplate" TargetType="{x:Type TreeViewItem}">
                <Grid SnapsToDevicePixels="True">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <ToggleButton x:Name="Expander"
                                  Grid.Row="0"
                                  Grid.Column="0"
                                  Margin="0,0,6,0"
                                  VerticalAlignment="Center"
                                  Focusable="False"
                                  ClickMode="Press"
                                  IsChecked="{Binding IsExpanded, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}"
                                  Style="{StaticResource ExpandCollapseToggleStyle}">
                        <Grid x:Name="ArrowContainer"
                              Width="16"
                              Height="16"
                              RenderTransformOrigin="0.5,0.5">
                            <Grid.RenderTransform>
                                <RotateTransform Angle="-90" />
                            </Grid.RenderTransform>
                            <ui:SymbolIcon x:Name="ArrowGlyph"
                                          Symbol="ArrowSortDown16"
                                          Filled="True"
                                          Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                          HorizontalAlignment="Center"
                                          VerticalAlignment="Center" />
                        </Grid>
                    </ToggleButton>
                    <Border x:Name="HeaderBorder"
                            Grid.Row="0"
                            Grid.Column="1"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter ContentSource="Header"
                                          HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
                    </Border>
                    <ItemsPresenter x:Name="ItemsHost"
                                    Grid.Row="1"
                                    Grid.Column="1"
                                    Margin="18,0,0,0"
                                    Visibility="Collapsed" />
                </Grid>
                <ControlTemplate.Triggers>
                    <Trigger Property="HasItems" Value="False">
                        <Setter TargetName="Expander" Property="Visibility" Value="Hidden" />
                    </Trigger>
                    <Trigger Property="IsExpanded" Value="True">
                        <Setter TargetName="ItemsHost" Property="Visibility" Value="Visible" />
                    </Trigger>
                    <Trigger Property="IsExpanded" Value="False">
                        <Setter TargetName="ItemsHost" Property="Visibility" Value="Collapsed" />
                    </Trigger>
                    <Trigger Property="IsEnabled" Value="False">
                        <Setter TargetName="HeaderBorder" Property="Opacity" Value="0.6" />
                        <Setter TargetName="ArrowGlyph" Property="Opacity" Value="0.5" />
                    </Trigger>
                </ControlTemplate.Triggers>
            </ControlTemplate>

            <!-- Tree styles: avoid binding errors on leaf nodes + keep scroll performance predictable -->
            <Style x:Key="ChooseTreeCategoryItemStyle"
                   TargetType="{x:Type TreeViewItem}"
                   BasedOn="{StaticResource {x:Type TreeViewItem}}">
                <Setter Property="IsExpanded" Value="{Binding IsExpanded, Mode=TwoWay}" />
                <Setter Property="Visibility" Value="{Binding IsVisible, Converter={StaticResource BoolToVisibility}}" />
                <Setter Property="VirtualizingStackPanel.IsVirtualizing" Value="True" />
                <Setter Property="VirtualizingStackPanel.VirtualizationMode" Value="Recycling" />
                <Setter Property="VirtualizingStackPanel.ScrollUnit" Value="Item" />
                <Setter Property="VirtualizingStackPanel.CacheLength" Value="2" />
                <Setter Property="VirtualizingStackPanel.CacheLengthUnit" Value="Page" />
                <Setter Property="Background" Value="Transparent" />
                <Setter Property="BorderThickness" Value="0" />
                <Setter Property="BorderBrush" Value="Transparent" />
                <Setter Property="Padding" Value="6,2" />
                <Setter Property="Margin" Value="0" />
                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                <Setter Property="VerticalContentAlignment" Value="Center" />
                <Setter Property="FocusVisualStyle" Value="{x:Null}" />
                <Setter Property="Focusable" Value="False" />
                <Setter Property="IsTabStop" Value="False" />
                <Setter Property="Template" Value="{StaticResource ChooseTreeItemTemplate}" />
                <EventSetter Event="Selected" Handler="ChooseTreeItem_Selected" />
                <EventSetter Event="Loaded" Handler="ChooseTreeItem_Loaded" />
                <EventSetter Event="Expanded" Handler="ChooseTreeItem_Expanded" />
                <EventSetter Event="Collapsed" Handler="ChooseTreeItem_Collapsed" />
                <Style.Triggers>
                    <MultiDataTrigger>
                        <MultiDataTrigger.Conditions>
                            <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=IsMouseOver}" Value="True" />
                            <Condition Binding="{Binding DataContext.IsChooseTreeScrolling, RelativeSource={RelativeSource AncestorType=TreeView}}" Value="False" />
                        </MultiDataTrigger.Conditions>
                        <Setter Property="Background" Value="{DynamicResource ControlFillColorTertiaryBrush}" />
                    </MultiDataTrigger>
                </Style.Triggers>
            </Style>

            <Style x:Key="ChooseTreePropertyItemStyle"
                   TargetType="{x:Type TreeViewItem}"
                   BasedOn="{StaticResource {x:Type TreeViewItem}}">
                <Setter Property="VirtualizingStackPanel.IsVirtualizing" Value="True" />
                <Setter Property="VirtualizingStackPanel.VirtualizationMode" Value="Recycling" />
                <Setter Property="VirtualizingStackPanel.ScrollUnit" Value="Item" />
                <Setter Property="VirtualizingStackPanel.CacheLength" Value="2" />
                <Setter Property="VirtualizingStackPanel.CacheLengthUnit" Value="Page" />
                <Setter Property="Background" Value="Transparent" />
                <Setter Property="BorderThickness" Value="0" />
                <Setter Property="BorderBrush" Value="Transparent" />
                <Setter Property="Padding" Value="6,1" />
                <Setter Property="Margin" Value="0" />
                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                <Setter Property="VerticalContentAlignment" Value="Center" />
                <Setter Property="FocusVisualStyle" Value="{x:Null}" />
                <Setter Property="Focusable" Value="False" />
                <Setter Property="IsTabStop" Value="False" />
                <Setter Property="Template" Value="{StaticResource ChooseTreeItemTemplate}" />
                <EventSetter Event="Selected" Handler="ChooseTreeItem_Selected" />
                <EventSetter Event="Loaded" Handler="ChooseTreeItem_Loaded" />
                <EventSetter Event="Expanded" Handler="ChooseTreeItem_Expanded" />
                <EventSetter Event="Collapsed" Handler="ChooseTreeItem_Collapsed" />
                <Style.Triggers>
                    <MultiDataTrigger>
                        <MultiDataTrigger.Conditions>
                            <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=IsMouseOver}" Value="True" />
                            <Condition Binding="{Binding DataContext.IsChooseTreeScrolling, RelativeSource={RelativeSource AncestorType=TreeView}}" Value="False" />
                        </MultiDataTrigger.Conditions>
                        <Setter Property="Background" Value="{DynamicResource ControlFillColorTertiaryBrush}" />
                    </MultiDataTrigger>
                </Style.Triggers>
            </Style>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="{x:Static local:MicroEngResourceUris.WpfUiRoot}" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ui:Card Grid.Row="0" Padding="10" Margin="0,0,0,8">
            <StackPanel>
                <ui:TextBlock Text="Column Builder" FontSize="18" FontWeight="SemiBold" />
                <ui:TextBlock Text="Choose columns by category and confirm which headers to include."
                              Margin="0,4,0,0"
                              Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
            </StackPanel>
        </ui:Card>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="12"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <ui:Card Grid.Column="0"
                     Padding="10"
                     HorizontalContentAlignment="Stretch"
                     VerticalContentAlignment="Top"
                     VerticalAlignment="Stretch">
                <DockPanel>
                    <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,8">
                        <ui:TextBlock Text="Choose Columns" FontWeight="SemiBold" />
                        <ui:TextBlock Text=" - " Margin="8,0,0,0" FontWeight="SemiBold" />
                        <ui:TextBlock Text="{Binding AvailableCount}"
                                      FontWeight="SemiBold"
                                      Foreground="{StaticResource ColumnBuilderCountBrush}" />
                        <ui:TextBlock Text=" Available" FontWeight="SemiBold" />
                    </StackPanel>
                    <StackPanel DockPanel.Dock="Top" Margin="0,0,0,8">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Grid Grid.Column="0">
                                <TextBox x:Name="ChooseCategoryFilterTextBox"
                                         Padding="8,4"
                                         Text="{Binding CategoryFilterText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                         TextChanged="ChooseFilterTextBox_TextChanged" />
                                <TextBlock Text="Search category..."
                                           Margin="12,0,0,0"
                                           VerticalAlignment="Center"
                                           Foreground="{DynamicResource TextPlaceholderColorBrush}"
                                           IsHitTestVisible="False">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Visibility" Value="Collapsed" />
                                            <Style.Triggers>
                                                <MultiDataTrigger>
                                                    <MultiDataTrigger.Conditions>
                                                        <Condition Binding="{Binding Text, ElementName=ChooseCategoryFilterTextBox}" Value="" />
                                                        <Condition Binding="{Binding IsKeyboardFocusWithin, ElementName=ChooseCategoryFilterTextBox}" Value="False" />
                                                    </MultiDataTrigger.Conditions>
                                                    <Setter Property="Visibility" Value="Visible" />
                                                </MultiDataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </Grid>
                        </Grid>
                        <Grid Margin="0,6,0,0">
                            <TextBox x:Name="ChoosePropertyFilterTextBox"
                                     Padding="8,4"
                                     Text="{Binding PropertyFilterText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                     TextChanged="ChooseFilterTextBox_TextChanged" />
                            <TextBlock Text="Search property..."
                                       Margin="12,0,0,0"
                                       VerticalAlignment="Center"
                                       Foreground="{DynamicResource TextPlaceholderColorBrush}"
                                       IsHitTestVisible="False">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Visibility" Value="Collapsed" />
                                        <Style.Triggers>
                                            <MultiDataTrigger>
                                                <MultiDataTrigger.Conditions>
                                                    <Condition Binding="{Binding Text, ElementName=ChoosePropertyFilterTextBox}" Value="" />
                                                    <Condition Binding="{Binding IsKeyboardFocusWithin, ElementName=ChoosePropertyFilterTextBox}" Value="False" />
                                                </MultiDataTrigger.Conditions>
                                                <Setter Property="Visibility" Value="Visible" />
                                            </MultiDataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </Grid>
                        <ui:ToggleSwitch x:Name="FilterBySelectionToggle"
                                         Content="Filter by selection"
                                         Margin="0,8,0,0"
                                         OffContent="Filter by selection"
                                         OnContent="Filter by selection"
                                         IsChecked="{Binding FilterBySelection, Mode=TwoWay}" />
                    </StackPanel>
                    <Grid>
                    <TreeView x:Name="ChooseTree"
                              ItemsSource="{Binding Categories}"
                              Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                              Background="Transparent"
                              BorderThickness="0"
                              BorderBrush="Transparent"
                              ScrollViewer.CanContentScroll="True"
                              ScrollViewer.IsDeferredScrollingEnabled="True"
                              VirtualizingStackPanel.IsVirtualizing="True"
                              VirtualizingStackPanel.VirtualizationMode="Recycling"
                              VirtualizingStackPanel.CacheLength="2"
                              VirtualizingStackPanel.CacheLengthUnit="Page"
                              VirtualizingStackPanel.ScrollUnit="Item"
                              ItemContainerStyle="{StaticResource ChooseTreeCategoryItemStyle}">
                            <TreeView.Style>
                                <Style TargetType="TreeView">
                                    <Setter Property="Visibility" Value="Visible" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsFilterActive}" Value="True">
                                            <Setter Property="Visibility" Value="Collapsed" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TreeView.Style>
<TreeView.Resources>
                                <HierarchicalDataTemplate DataType="{x:Type local:ColumnBuilderCategoryNode}" ItemsSource="{Binding PropertiesView}" ItemContainerStyle="{StaticResource ChooseTreePropertyItemStyle}">
                                    <CheckBox Style="{StaticResource ColumnBuilderTreeCheckBoxStyle}" Content="{Binding Name}"
                                              IsThreeState="True"
                                              IsChecked="{Binding IsChecked, Mode=TwoWay}"
                                              PreviewMouseLeftButtonDown="CategoryCheckBox_PreviewMouseLeftButtonDown"
                                              PreviewKeyDown="CategoryCheckBox_PreviewKeyDown" />
                                </HierarchicalDataTemplate>
                                <DataTemplate DataType="{x:Type local:ColumnBuilderPropertyNode}">
                                    <CheckBox Style="{StaticResource ColumnBuilderTreeCheckBoxStyle}" Content="{Binding PropertyName}"
                                              IsThreeState="False"
                                              IsChecked="{Binding IsChecked, Mode=TwoWay}" />
                                </DataTemplate>
                            </TreeView.Resources>
                        </TreeView>

                        <ListBox x:Name="ChooseFilterResults"
                                 ItemsSource="{Binding FilteredProperties}"
                                 SelectionMode="Single"
                                 Background="Transparent"
                                 BorderThickness="0"
                                 BorderBrush="Transparent"
                                 ScrollViewer.CanContentScroll="True"
                                 ScrollViewer.IsDeferredScrollingEnabled="True"
                                 VirtualizingStackPanel.IsVirtualizing="True"
                                 VirtualizingStackPanel.VirtualizationMode="Recycling"
                                 VirtualizingStackPanel.CacheLength="2"
                                 VirtualizingStackPanel.CacheLengthUnit="Page"
                                 VirtualizingStackPanel.ScrollUnit="Item">
                            <ListBox.Style>
                                <Style TargetType="ListBox">
                                    <Setter Property="Visibility" Value="Collapsed" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsFilterActive}" Value="True">
                                            <Setter Property="Visibility" Value="Visible" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </ListBox.Style>
                            <ListBox.ItemContainerStyle>
                                <Style TargetType="ListBoxItem">
                                    <Setter Property="Background" Value="Transparent" />
                                    <Setter Property="BorderBrush" Value="Transparent" />
                                    <Setter Property="BorderThickness" Value="0" />
                                    <Setter Property="Foreground" Value="{DynamicResource TextFillColorPrimaryBrush}" />
                                    <Setter Property="Padding" Value="6,2" />
                                    <Setter Property="Margin" Value="0" />
                                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                                    <Setter Property="FocusVisualStyle" Value="{x:Null}" />
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="{DynamicResource ControlFillColorTertiaryBrush}" />
                                        </Trigger>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter Property="Background" Value="{DynamicResource ControlFillColorTertiaryBrush}" />
                                            <Setter Property="BorderBrush" Value="{DynamicResource AccentFillColorDefaultBrush}" />
                                            <Setter Property="BorderThickness" Value="3,0,0,0" />
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </ListBox.ItemContainerStyle>
                            <ListBox.ItemTemplate>
                                <DataTemplate DataType="{x:Type local:ColumnBuilderPropertyNode}">
                                    <StackPanel>
                                        <CheckBox Style="{StaticResource ColumnBuilderTreeCheckBoxStyle}" IsThreeState="False"
                                                  IsChecked="{Binding IsChecked, Mode=TwoWay}"
                                                  Content="{Binding PropertyName}"
                                                  Foreground="{DynamicResource TextFillColorPrimaryBrush}" />
                                        <TextBlock Text="{Binding CategoryName}"
                                                   FontSize="11"
                                                   Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                                   Margin="22,0,0,0" />
                                    </StackPanel>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </Grid>
                </DockPanel>
            </ui:Card>

            <ui:Card Grid.Column="2"
                     Padding="10"
                     HorizontalContentAlignment="Stretch"
                     VerticalContentAlignment="Top"
                     VerticalAlignment="Stretch">
                <DockPanel>
                    <StackPanel DockPanel.Dock="Top" Margin="0,0,0,8">
                        <StackPanel Orientation="Horizontal">
                            <ui:TextBlock Text="Chosen Columns" FontWeight="SemiBold" />
                            <ui:TextBlock Text=" - " Margin="8,0,0,0" FontWeight="SemiBold" />
                            <ui:TextBlock Text="{Binding SelectedCountValue}"
                                          FontWeight="SemiBold"
                                          Foreground="{StaticResource ColumnBuilderCountBrush}" />
                            <ui:TextBlock Text="{Binding SelectedCountSuffix}" FontWeight="SemiBold" />
                        </StackPanel>
                        <Grid Margin="0,8,0,0">
                            <TextBox x:Name="SelectedFilterTextBox"
                                     Padding="8,4"
                                     Text="{Binding SelectedFilterText, UpdateSourceTrigger=PropertyChanged}" />
                            <TextBlock Text="Search chosen columns..."
                                       Margin="12,0,0,0"
                                       VerticalAlignment="Center"
                                       Foreground="{DynamicResource TextPlaceholderColorBrush}"
                                       IsHitTestVisible="False">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Visibility" Value="Collapsed" />
                                        <Style.Triggers>
                                            <MultiDataTrigger>
                                                <MultiDataTrigger.Conditions>
                                                    <Condition Binding="{Binding Text, ElementName=SelectedFilterTextBox}" Value="" />
                                                    <Condition Binding="{Binding IsKeyboardFocusWithin, ElementName=SelectedFilterTextBox}" Value="False" />
                                                </MultiDataTrigger.Conditions>
                                                <Setter Property="Visibility" Value="Visible" />
                                            </MultiDataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </Grid>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,8,0,0">
                            <ui:Button x:Name="MoveSelectedUpButton" Width="32" Height="28" Padding="0" Margin="0,0,6,0" ToolTip="Move up">
                                <ui:SymbolIcon Symbol="ArrowSortUp16" Filled="True" />
                            </ui:Button>
                            <ui:Button x:Name="MoveSelectedDownButton" Width="32" Height="28" Padding="0" Margin="0,0,6,0" ToolTip="Move down">
                                <ui:SymbolIcon Symbol="ArrowSortDown16" Filled="True" />
                            </ui:Button>
                            <ui:Button x:Name="DeselectSelectedButton" Width="32" Height="28" Padding="0" Margin="0,0,6,0" ToolTip="Deselect">
                                <ui:SymbolIcon Symbol="FilterDismiss16" Filled="True" />
                            </ui:Button>
                            <ui:Button x:Name="ClearAllSelectedButton" Width="32" Height="28" Padding="0" ToolTip="Clear all">
                                <ui:SymbolIcon Symbol="Delete16" Filled="True" />
                            </ui:Button>
                        </StackPanel>
                    </StackPanel>

                    <ListBox x:Name="SelectedListBox"
                             ItemsSource="{Binding SelectedProperties}"
                             SelectedItem="{Binding SelectedItem, Mode=TwoWay}"
                             SelectionMode="Single"
                             AllowDrop="True"
                             Padding="0,0,10,0"
                             ScrollViewer.VerticalScrollBarVisibility="Auto"
                             ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                             PreviewMouseLeftButtonUp="SelectedListBox_PreviewMouseLeftButtonUp"
                             PreviewMouseLeftButtonDown="SelectedListBox_PreviewMouseLeftButtonDown"
                             MouseMove="SelectedListBox_MouseMove"
                             DragOver="SelectedListBox_DragOver"
                             Drop="SelectedListBox_Drop">
                        <ListBox.ItemContainerStyle>
                            <Style TargetType="ListBoxItem">
                                <Setter Property="Background" Value="Transparent" />
                                <Setter Property="BorderBrush" Value="Transparent" />
                                <Setter Property="BorderThickness" Value="0" />
                                <Setter Property="Padding" Value="2,1" />
                                <Setter Property="Margin" Value="0" />
                                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                                <Setter Property="VerticalContentAlignment" Value="Center" />
                                <Setter Property="FocusVisualStyle" Value="{x:Null}" />
                                <Setter Property="TextElement.FontSize" Value="11" />
                                <Setter Property="TextElement.FontWeight" Value="Normal" />
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="ListBoxItem">
                                            <Border x:Name="Bd"
                                                    Background="{TemplateBinding Background}"
                                                    BorderBrush="{TemplateBinding BorderBrush}"
                                                    BorderThickness="{TemplateBinding BorderThickness}"
                                                    SnapsToDevicePixels="True">
                                                <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                                  VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                                                  SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="Bd" Property="Background" Value="{DynamicResource ControlFillColorTertiaryBrush}" />
                                                </Trigger>
                                                <Trigger Property="IsSelected" Value="True">
                                                    <Setter TargetName="Bd" Property="Background" Value="{DynamicResource ControlFillColorTertiaryBrush}" />
                                                    <Setter TargetName="Bd" Property="BorderBrush" Value="{DynamicResource AccentFillColorDefaultBrush}" />
                                                    <Setter TargetName="Bd" Property="BorderThickness" Value="3,0,0,0" />
                                                    <Setter Property="TextElement.FontWeight" Value="Bold" />
                                                </Trigger>
                                                <Trigger Property="IsEnabled" Value="False">
                                                    <Setter TargetName="Bd" Property="Opacity" Value="0.6" />
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </ListBox.ItemContainerStyle>
                        <ListBox.ItemTemplate>
                            <DataTemplate DataType="{x:Type local:ColumnBuilderPropertyNode}">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <CheckBox Grid.Column="0"
                                              Style="{StaticResource ColumnBuilderTreeCheckBoxStyle}"
                                              IsChecked="{Binding IsChecked, Mode=TwoWay}"
                                              IsThreeState="False"
                                              Margin="4,0,8,0"
                                              VerticalAlignment="Center" />
                                    <Border Grid.Column="1"
                                            Width="22"
                                            Height="18"
                                            CornerRadius="3"
                                            Margin="0,0,8,0"
                                            Background="{DynamicResource CardBackgroundFillColorDefaultBrush}"
                                            BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}"
                                            BorderThickness="1"
                                            VerticalAlignment="Center">
                                        <TextBlock Text="{Binding OrderIndex}"
                                                   HorizontalAlignment="Center"
                                                   VerticalAlignment="Center"
                                                   FontSize="11"
                                                   Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                                    </Border>
                                    <StackPanel Grid.Column="2" Margin="0,0,6,0" Orientation="Horizontal">
                                        <TextBlock Text="{Binding PropertyName}"
                                                   Margin="0,0,6,0"
                                                   VerticalAlignment="Center"
                                                   FontSize="12">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Foreground" Value="{StaticResource ColumnBuilderCountBrush}" />
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsRequired}" Value="True">
                                                            <Setter Property="Foreground" Value="{StaticResource ColumnBuilderRequiredBrush}" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                        <TextBlock Text="{Binding CategoryName}"
                                                   FontSize="12"
                                                   VerticalAlignment="Center"
                                                   Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                                    </StackPanel>
                                    <ui:ToggleSwitch Grid.Column="3"
                                                     Margin="16,0,16,0"
                                                     VerticalAlignment="Center"
                                                     Content="Required"
                                                     IsChecked="{Binding IsRequired, Mode=TwoWay}" />
                                </Grid>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </DockPanel>
            </ui:Card>
        </Grid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <ui:Button Content="Cancel" Width="90" Margin="0,0,8,0" IsCancel="True" />
            <ui:Button Content="Apply" Width="110" IsDefault="True" Click="Apply_Click"/>
        </StackPanel>
    </Grid>
</Window>
