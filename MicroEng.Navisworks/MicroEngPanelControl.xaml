<UserControl x:Class="MicroEng.Navisworks.MicroEngPanelControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             mc:Ignorable="d"
             d:DesignHeight="520" d:DesignWidth="380"
             MinWidth="360"
             MinHeight="520"
             Background="{DynamicResource ApplicationBackgroundBrush}">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
            <ResourceDictionary Source="pack://application:,,,/MicroEng.Navisworks;component/Assets/Theme/MicroEngUiKit.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <ui:Card Grid.Row="0" Padding="10" Margin="0,0,0,8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="160" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Image Source="pack://siteoforigin:,,,/Logos/microeng-logo2.png"
                       Height="32"
                       Stretch="Uniform"
                       HorizontalAlignment="Left"
                       VerticalAlignment="Center" />
                <TextBlock Grid.Column="1"
                           Text="MicroEng Tools"
                           FontSize="14"
                           FontWeight="SemiBold"
                           VerticalAlignment="Center"
                           Foreground="{StaticResource TextPrimary}"
                           HorizontalAlignment="Left"
                           Margin="12,0,0,0" />
            </Grid>
        </ui:Card>

        <ui:Card Grid.Row="1" Padding="10" Margin="0,0,0,8">
            <StackPanel>
                <ui:Button Content="Data Scraper" Appearance="Secondary" Margin="0,4" Click="DataScraper_Click"/>
                <ui:Button Content="Data Mapper" Appearance="Primary" Margin="0,4" Click="AppendData_Click"/>
                <ui:Button Content="Data Matrix" Appearance="Secondary" Margin="0,4" Click="DataMatrix_Click"/>
                <ui:Button Content="Space Mapper" Appearance="Secondary" Margin="0,4" Click="SpaceMapper_Click"/>
            </StackPanel>
        </ui:Card>

        <ui:Card Grid.Row="2" Padding="10" Margin="0,0,0,0">
            <StackPanel>
                <TextBlock Text="Log"
                           FontWeight="SemiBold"
                           Margin="0,0,0,6"
                           Foreground="{StaticResource TextPrimary}" />
            <TextBox x:Name="LogTextBox"
                     Text="Ready"
                     AcceptsReturn="True"
                     IsReadOnly="True"
                     VerticalScrollBarVisibility="Auto"
                     BorderBrush="#d0d7de"
                     BorderThickness="1"
                     Background="White"
                     FontFamily="Segoe UI"
                     FontSize="11" />
            </StackPanel>
        </ui:Card>
    </Grid>
</UserControl>
