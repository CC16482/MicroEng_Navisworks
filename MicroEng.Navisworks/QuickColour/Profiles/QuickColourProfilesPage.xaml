<UserControl x:Class="MicroEng.Navisworks.QuickColour.Profiles.QuickColourProfilesPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:local="clr-namespace:MicroEng.Navisworks.QuickColour.Profiles"
             MinWidth="820"
             Background="{DynamicResource ApplicationBackgroundBrush}"
             Foreground="{DynamicResource TextFillColorPrimaryBrush}"
             FontSize="12">
    <UserControl.Resources>
        <local:ColorHexToBrushConverter x:Key="ColorHexToBrushConverter" />
    </UserControl.Resources>
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ui:Card Grid.Row="0"
                 Padding="10"
                 Margin="0,0,0,8"
                 HorizontalContentAlignment="Stretch">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel>
                    <ui:TextBlock Text="Profiles" FontSize="16" FontWeight="SemiBold" />
                    <ui:TextBlock Text="Saved MicroEng Colour Profiles (from Quick Colour / Hierarchy Builder)."
                                  Margin="0,4,0,0"
                                  FontSize="12"
                                  Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                </StackPanel>

                <WrapPanel Grid.Column="1" HorizontalAlignment="Right">
                    <ui:Button Content="Refresh" Width="90" Margin="0,0,8,0" Click="Refresh_Click" />
                    <ui:Button Content="Import" Width="90" Margin="0,0,8,0" Click="Import_Click" />
                    <ui:Button Content="Export" Width="90" Margin="0,0,8,0" Click="Export_Click" />
                    <ui:Button Content="Delete" Width="90" Click="Delete_Click" />
                </WrapPanel>
            </Grid>
        </ui:Card>

        <ui:Card Grid.Row="1"
                 Padding="10"
                 Margin="0,0,0,8"
                 HorizontalContentAlignment="Stretch"
                 VerticalContentAlignment="Stretch"
                 VerticalAlignment="Stretch">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <ui:TextBlock Text="Library" FontSize="13" FontWeight="SemiBold" Margin="0,0,0,6" />
                <ui:DataGrid Grid.Row="1"
                             ItemsSource="{Binding Profiles}"
                             SelectedItem="{Binding SelectedProfile, Mode=TwoWay}"
                             AutoGenerateColumns="False"
                             CanUserAddRows="False"
                             IsReadOnly="True"
                             HeadersVisibility="Column"
                             RowHeight="26"
                             FontSize="12"
                             GridLinesVisibility="All"
                             HorizontalGridLinesBrush="{DynamicResource MicroEngDataGridGridLineBrush}"
                             VerticalGridLinesBrush="{DynamicResource MicroEngDataGridGridLineBrush}">
                    <ui:DataGrid.Columns>
                        <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="2*" />
                        <DataGridTextColumn Header="Source" Binding="{Binding Source}" Width="140" />
                        <DataGridTextColumn Header="Category" Binding="{Binding Generator.CategoryName}" Width="160" />
                        <DataGridTextColumn Header="Property" Binding="{Binding Generator.PropertyName}" Width="160" />
                        <DataGridTextColumn Header="Modified" Binding="{Binding ModifiedUtc}" Width="180" />
                    </ui:DataGrid.Columns>
                </ui:DataGrid>
            </Grid>
        </ui:Card>

        <ui:Card Grid.Row="2"
                 Padding="10"
                 Margin="0,0,0,8"
                 HorizontalContentAlignment="Stretch"
                 VerticalContentAlignment="Stretch"
                 VerticalAlignment="Stretch">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <ui:TextBlock Text="Mapping Preview" FontSize="13" FontWeight="SemiBold" Margin="0,0,0,6" />
                <ui:DataGrid Grid.Row="1"
                             ItemsSource="{Binding SelectedProfile.Rules}"
                             AutoGenerateColumns="False"
                             CanUserAddRows="False"
                             IsReadOnly="True"
                             HeadersVisibility="Column"
                             RowHeight="26"
                             FontSize="12"
                             GridLinesVisibility="All"
                             HorizontalGridLinesBrush="{DynamicResource MicroEngDataGridGridLineBrush}"
                             VerticalGridLinesBrush="{DynamicResource MicroEngDataGridGridLineBrush}">
                    <ui:DataGrid.Columns>
                        <DataGridCheckBoxColumn Header="On" Binding="{Binding Enabled}" Width="60" />
                        <DataGridTemplateColumn Header="Color" Width="64">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Border Background="{Binding ColorHex, Converter={StaticResource ColorHexToBrushConverter}}"
                                            BorderBrush="Gray" BorderThickness="1"
                                            Height="16" Width="48" CornerRadius="2" />
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTextColumn Header="Value" Binding="{Binding Value}" Width="2*" />
                        <DataGridTextColumn Header="Hex" Binding="{Binding ColorHex}" Width="140" />
                        <DataGridTextColumn Header="Count" Binding="{Binding Count}" Width="120" />
                    </ui:DataGrid.Columns>
                </ui:DataGrid>
            </Grid>
        </ui:Card>

        <ui:Card Grid.Row="3"
                 Padding="10"
                 HorizontalContentAlignment="Stretch"
                 VerticalContentAlignment="Center">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock Text="{Binding StatusText}"
                           Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                           VerticalAlignment="Center" />
                <WrapPanel Grid.Column="1">
                    <ui:Button Content="Apply Temporary"
                               Width="140"
                               Margin="0,0,8,0"
                               IsEnabled="{Binding HasSelection}"
                               Click="ApplyTemporary_Click" />
                    <ui:Button Content="Apply Permanent"
                               Width="140"
                               IsEnabled="{Binding HasSelection}"
                               Click="ApplyPermanent_Click" />
                </WrapPanel>
            </Grid>
        </ui:Card>
    </Grid>
</UserControl>
