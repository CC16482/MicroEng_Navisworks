<ui:FluentWindow x:Class="MicroEng.Navisworks.SpaceMapperRunProgressWindow"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                 xmlns:local="clr-namespace:MicroEng.Navisworks"
                 x:ClassModifier="internal"
                 Title="Space Mapper"
                 Width="520"
                 Height="240"
                 ResizeMode="NoResize"
                 WindowStartupLocation="CenterScreen"
                 Background="{DynamicResource ApplicationBackgroundBrush}"
                 Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                 FontSize="12">
  <ui:FluentWindow.Resources>
    <ResourceDictionary>
      <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="{x:Static local:MicroEngResourceUris.WpfUiRoot}" />
      </ResourceDictionary.MergedDictionaries>
    </ResourceDictionary>
  </ui:FluentWindow.Resources>

  <Grid Margin="20">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>

    <StackPanel Grid.Row="0" Margin="0,0,0,12">
      <ui:TextBlock x:Name="HeaderText"
                    Text="Running Space Mapper"
                    FontSize="18"
                    FontWeight="SemiBold" />
      <ui:TextBlock x:Name="SubHeaderText"
                    Text="Please wait. You can leave this window open while processing completes."
                    Opacity="0.75"
                    TextWrapping="Wrap" />
    </StackPanel>

    <ui:Card Grid.Row="1" Padding="14">
      <Grid>
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="Auto" />
          <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <ui:ProgressRing Width="52"
                         Height="52"
                         IsIndeterminate="True"
                         VerticalAlignment="Top" />

        <StackPanel Grid.Column="1" Margin="16,0,0,0">
          <ui:TextBlock x:Name="StageTextBlock"
                        Text="Starting..."
                        FontSize="14"
                        FontWeight="SemiBold" />

          <ui:TextBlock x:Name="DetailTextBlock"
                        Text="-"
                        Margin="0,6,0,0"
                        Opacity="0.75"
                        TextWrapping="Wrap" />

          <ui:TextBlock x:Name="StatsTextBlock"
                        Text="-"
                        Margin="0,10,0,0"
                        Opacity="0.75"
                        TextWrapping="Wrap" />
        </StackPanel>
      </Grid>
    </ui:Card>

    <StackPanel Grid.Row="2"
                Orientation="Horizontal"
                HorizontalAlignment="Right"
                Margin="0,12,0,0">
      <ui:Button x:Name="CancelButton"
                 Content="Cancel"
                 Appearance="Secondary"
                 Click="CancelButton_OnClick" />
    </StackPanel>
  </Grid>
</ui:FluentWindow>
