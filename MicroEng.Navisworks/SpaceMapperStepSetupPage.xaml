<Page x:Class="MicroEng.Navisworks.SpaceMapperStepSetupPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:local="clr-namespace:MicroEng.Navisworks"
      xmlns:system="clr-namespace:System;assembly=System"
      mc:Ignorable="d"
      d:DesignHeight="600"
      d:DesignWidth="900"
      Background="Transparent"
      Foreground="{DynamicResource TextFillColorPrimaryBrush}"
      FontSize="12">

  <Page.Resources>
    <ResourceDictionary>
      <system:Double x:Key="ControlContentThemeFontSize">12</system:Double>
      <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="{x:Static local:MicroEngResourceUris.WpfUiRoot}" />
      </ResourceDictionary.MergedDictionaries>
    </ResourceDictionary>
  </Page.Resources>

  <Grid Margin="12">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>

    <ui:Card Grid.Row="0"
             Padding="10"
             Margin="0,0,0,8"
             HorizontalAlignment="Stretch"
             HorizontalContentAlignment="Stretch">
      <StackPanel>
        <ui:TextBlock Text="Step 1 - Setup" FontSize="16" FontWeight="SemiBold" />
        <ui:TextBlock Text="Choose a Data Scraper profile and scope before running Space Mapper."
                      Margin="0,4,0,0"
                      FontSize="12"
                      Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
      </StackPanel>
    </ui:Card>

    <ui:Card Grid.Row="1"
             Padding="10"
             Margin="0,0,0,8"
             HorizontalAlignment="Stretch"
             HorizontalContentAlignment="Stretch">
      <Grid>
        <Grid.RowDefinitions>
          <RowDefinition Height="Auto" />
          <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Grid.Row="0">
          <TextBlock Text="Profile:" VerticalAlignment="Center" Margin="0,0,6,0" />
          <ComboBox x:Name="ProfileComboBox" Width="180" Margin="0,0,8,0" />
          <ui:Button x:Name="RefreshProfilesButtonControl" Content="Refresh" />
        </StackPanel>

        <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="0,8,0,0">
          <TextBlock Text="Scope:" VerticalAlignment="Center" Margin="0,0,6,0" />
          <ComboBox x:Name="ScopeComboBox" Width="160" />
        </StackPanel>
      </Grid>
    </ui:Card>

    <ui:Card Grid.Row="2"
             Padding="10"
             HorizontalAlignment="Stretch"
             HorizontalContentAlignment="Stretch">
      <Grid>
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="*" />
          <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <StackPanel>
          <ui:TextBlock Text="Actions" FontSize="13" FontWeight="SemiBold" />
          <ui:TextBlock Text="Run the Data Scraper if you need a fresh profile, then run Space Mapper."
                        Margin="0,4,0,0"
                        FontSize="12"
                        Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
        </StackPanel>

        <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
          <ui:Button x:Name="RunScraperButtonControl" Content="Run Data Scraper..." Margin="0,0,8,0" />
          <ui:Button x:Name="RunButtonControl" Content="Run Space Mapper" Appearance="Primary" />
        </StackPanel>
      </Grid>
    </ui:Card>
  </Grid>
</Page>
